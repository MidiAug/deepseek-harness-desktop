# 配置

> 状态：设置 UI 随 ROADMAP B3–B5 落地；下列为产品约定，实现后与界面字段对齐。

## 网络

| 项 | 说明 |
|----|------|
| HTTP / SOCKS / 系统代理 | 用于壳下载 harness，以及 `dsh plugin` 等需出网的操作 |
| 镜像 | 可配置下载镜像；**有镜像 ≠ 已配代理**，二者分开 |

代理建议在向导失败页或设置里随时可改，避免只能改系统环境变量。

## Harness 来源

本应用 **仅使用壳托管的 harness**（安装到应用数据目录，由本壳获取、监督与更新）。  
不提供「指向本机已有 `dsh`」的自带（BYO）模式。

若你以前用过官方 CLI：默认仍共用 `~/.dsh`，其中的会话与已装插件可被本壳启动的 harness 复用；**程序本身**仍是壳管理的那一份。

## 数据目录

默认与官方一致：`$DSH_HOME` → `~/.dsh`（Windows 上为用户目录下的 `.dsh`）。  
可在设置中覆盖，以便多套配置或便携场景。

卸载桌面端时：默认删除壳的程序与壳配置；**默认保留** `~/.dsh`。

## 版本信息

关于页应能看到：**壳版本** · **harness 版本** · **digest**（便于对照更新与排错）。壳与 harness 更新均由本应用负责。
